  <p><%= link_to project.name, project_show_path(project)  %></p>
  <p><%= project.description %></p>
  <%= image_tag project.thumb_image unless project.thumb_image.nil? %>
  <%= link_to "Edit", edit_project_path(project) %>
  <%= link_to "Remove Item", project_path(project), method: :delete, data: { confirm: 'Are you sure?' } %>