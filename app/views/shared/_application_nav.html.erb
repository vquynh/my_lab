
      <nav class="navbar">
        <h4 class="mylab"><%= link_to "MyLab", root_path %></h4>
        <nav class="nav nav-masthead">
          <%= link_to "Home", root_path, class: 'nav-link' %>
          <%= link_to "About", about_path, class: 'nav-link' %>
          <%= link_to "Contact", contact_path, class: 'nav-link' %>
          <%= link_to "Equipment", equipment_index_path, class: 'nav-link' %>
          <%= link_to "Projects", projects_path, class: 'nav-link' %>
          <%= link_to "My List", mylist_path, class: 'nav-link' if (logged_in?(:user) && !logged_in?(:labor_staff))%>
          <%= link_to "Booking", bookings_path, class: 'nav-link' if (logged_in?(:user) && !logged_in?(:labor_staff))%>
          <%= link_to "Booking", indexadmin_bookings_path, class: 'nav-link' if logged_in?(:labor_staff) %>
          <%= login_helper 'nav-link'  %>

        </nav>
    </nav>
 

    